# 📋 Web版報告書システム 実装ガイド

## システム概要

Excel動的生成型の報告書システム。Excelでテンプレートを作成し、Web上で入力、PDF/Excel出力が可能。

---

## 🚀 セットアップ手順

### 1. パッケージインストール

#### バックエンド（C# .NET Framework）

```powershell
# Visual Studio パッケージマネージャーコンソールで実行
Install-Package ClosedXML -Version 0.102.2
Install-Package FreeSpire.XLS -Version 12.11.0
Install-Package System.Drawing.Common -Version 8.0.0
Install-Package SixLabors.ImageSharp -Version 3.1.0
```

**注意**: FreeSpire.XLSは無料版（5シート、200行/シート制限）。本番環境では商用版Spire.XLSの購入を推奨。

```powershell
# 商用版（オプション）
Install-Package Spire.XLS
```

#### フロントエンド（React + TypeScript）

```bash
cd frontend

# 必須パッケージ
npm install react-hook-form@^7.49.3 @hookform/resolvers@^3.3.4 zod@^3.22.4
npm install konva@^9.3.3 react-konva@^18.2.10
npm install react-dropzone@^14.2.3
npm install date-fns@^3.2.0 react-datepicker@^4.25.0
npm install --save-dev @types/react-datepicker@^4.19.5
npm install clsx@^2.1.0 tailwind-merge@^2.2.0
```

### 2. データベースセットアップ

```powershell
# SQL Server Management Studioまたはコマンドラインで実行
sqlcmd -S localhost -d YourDatabase -i backend/Database/create_report_system_tables.sql
```

または、SQL Server Management Studioで直接実行：
1. `backend/Database/create_report_system_tables.sql` を開く
2. データベースを選択
3. 実行（F5）

### 3. ディレクトリ作成

```powershell
# PowerShellで実行
New-Item -Path "C:\app_data\templates" -ItemType Directory -Force
New-Item -Path "C:\app_data\images" -ItemType Directory -Force
New-Item -Path "C:\app_data\reports" -ItemType Directory -Force
```

### 4. Web.config 設定

`backend/Web.config` のデータベース接続文字列を確認：

```xml
<connectionStrings>
  <add name="MyDbConnection" 
       connectionString="Server=localhost;Database=YourDatabase;Trusted_Connection=True;" 
       providerName="System.Data.SqlClient" />
</connectionStrings>
```

---

## 📝 Excelテンプレートの作成方法

### プレースホルダー形式

```
{{フィールド名:型:オプション}}
```

### サポートされている型

| 型 | 説明 | 例 |
|---|---|---|
| `text` | テキスト | `{{worker_name:text}}` |
| `textarea` | 複数行テキスト | `{{work_content:textarea}}` |
| `number` | 数値 | `{{work_hours:number}}` |
| `date` | 日付 | `{{work_date:date}}` |
| `time` | 時刻 | `{{start_time:time}}` |
| `datetime` | 日時 | `{{created_at:datetime}}` |
| `select` | 選択肢 | `{{status:select:未着手,進行中,完了}}` |
| `email` | メールアドレス | `{{email:email}}` |
| `tel` | 電話番号 | `{{phone:tel}}` |
| `image` | 画像 | `{{photos:image}}` |

### テンプレート例

```
┌─────────────────────────────────────────────────┐
│                作業報告書                        │
├──────────────┬──────────────────────────────────┤
│ 報告番号     │ {{report_no:text}}               │
├──────────────┼──────────────────────────────────┤
│ 作業日       │ {{work_date:date}}               │
├──────────────┼──────────────────────────────────┤
│ 作業者       │ {{worker_name:text}}             │
├──────────────┼──────────────────────────────────┤
│ 作業場所     │ {{location:text}}                │
├──────────────┴──────────────────────────────────┤
│ 作業内容                                         │
├──────────────────────────────────────────────────┤
│ {{work_content:textarea}}                       │
│                                                  │
├──────────────────────────────────────────────────┤
│ 作業時間     │ {{work_hours:number}} 時間       │
├──────────────┴──────────────────────────────────┤
│ ステータス   │ {{status:select:未着手,進行中,完了}}│
├──────────────────────────────────────────────────┤
│ 添付写真                                         │
├──────────────────────────────────────────────────┤
│ {{photos:image}}                                │
│                                                  │
└──────────────────────────────────────────────────┘
```

実際のExcelでの作成：
1. Excelを開く
2. 好きなレイアウトで罫線を引く
3. 入力欄に `{{フィールド名:型}}` を記入
4. `.xlsx` 形式で保存

---

## 🔧 API エンドポイント

### テンプレート管理

#### テンプレート一覧取得
```
GET /api/templates
Query: ?isActive=true
```

#### テンプレート詳細取得
```
GET /api/templates/{id}
```

#### テンプレートアップロード
```
POST /api/templates/upload
Content-Type: multipart/form-data

Form Data:
  - template_name: "作業報告書"
  - template_code: "work_report"
  - description: "日次作業報告書"
  - created_user: "admin"
  - excel_file: [Excelファイル]
```

#### テンプレート更新
```
PUT /api/templates/{id}
Content-Type: application/json

{
  "template_name": "作業報告書（更新版）",
  "description": "更新された説明"
}
```

#### テンプレート削除
```
DELETE /api/templates/{id}
Query: ?deletedUser=admin
```

### 報告書管理

#### 報告書一覧取得
```
GET /api/reports
Query: ?status=draft&created_user=山田&date_from=2025-12-01&date_to=2025-12-31&page=1&limit=20
```

#### 報告書詳細取得
```
GET /api/reports/{id}
```

#### 報告書作成
```
POST /api/reports
Content-Type: application/json

{
  "template_id": 1,
  "created_user": "山田太郎",
  "data": {
    "work_date": "2025-12-16",
    "worker_name": "山田太郎",
    "location": "東京都渋谷区",
    "work_content": "配管点検作業を実施しました。",
    "work_hours": 4,
    "status": "完了"
  }
}
```

#### 報告書更新
```
PUT /api/reports/{id}
Content-Type: application/json

{
  "status": "submitted",
  "updated_user": "山田太郎",
  "data": {
    "work_date": "2025-12-16",
    "worker_name": "山田太郎",
    ...
  }
}
```

#### PDF生成・ダウンロード
```
GET /api/reports/{id}/pdf
Response: application/pdf
```

#### Excel生成・ダウンロード
```
GET /api/reports/{id}/excel
Response: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
```

#### PDFプレビュー
```
GET /api/reports/{id}/preview
Response: application/pdf (inline)
```

---

## 🎯 動作フロー

### 1. テンプレート登録（初回のみ）

```
管理者
  ↓ Excelでテンプレート作成
  ↓ POST /api/templates/upload
サーバー
  ↓ Excelファイル保存（C:\app_data\templates\）
  ↓ Excel解析（ClosedXML）
  ↓ フィールド定義抽出
  ↓ DB保存（t_report_templates, t_template_fields）
  ↓
完了（テンプレートID発行）
```

### 2. 報告書作成（日常業務）

```
ユーザー
  ↓ Webブラウザで入力フォーム表示
  ↓ データ入力
  ↓ POST /api/reports
サーバー
  ↓ 報告書番号生成（RPT-2025-0001）
  ↓ JSONでDB保存（t_reports）
  ↓
完了（報告書ID発行）
```

### 3. PDF出力

```
ユーザー
  ↓ GET /api/reports/{id}/pdf
サーバー
  ↓ DBから報告書データ取得
  ↓ テンプレートExcelをコピー（一時ファイル）
  ↓ データ埋め込み（ClosedXML）
  ↓ Excel → PDF変換（Spire.XLS）
  ↓ 一時Excelファイル削除
  ↓ PDFストリーム返却
ユーザー
  ↓ ダウンロード
```

---

## 📂 ファイル構造

```
backend/
├── Controllers/
│   ├── TemplateController.cs           # テンプレート管理API
│   └── ReportManagementController.cs   # 報告書管理API
├── Services/
│   ├── TemplateParserService.cs        # Excel解析サービス
│   └── PdfGenerationService.cs         # PDF生成サービス
├── Models/
│   ├── DTOs/
│   │   ├── ReportTemplateDto.cs        # テンプレートDTO
│   │   └── ReportDto.cs                # 報告書DTO
│   └── Repository/
│       ├── TemplateRepository.cs       # テンプレートDB操作
│       └── ReportRepository.cs         # 報告書DB操作
├── Database/
│   └── create_report_system_tables.sql # DDL
└── Web.config                          # 設定ファイル

C:/app_data/
├── templates/    # Excelテンプレート保存先
├── images/       # アップロード画像保存先
└── reports/      # 生成PDF保存先（オプション）
```

---

## 🧪 動作確認

### 1. テンプレートアップロードのテスト

Postmanまたはcurlで実行：

```bash
curl -X POST http://localhost:YOUR_PORT/api/templates/upload \
  -F "template_name=作業報告書" \
  -F "template_code=work_report_001" \
  -F "description=テスト用" \
  -F "created_user=admin" \
  -F "excel_file=@C:/path/to/template.xlsx"
```

### 2. 報告書作成のテスト

```bash
curl -X POST http://localhost:YOUR_PORT/api/reports \
  -H "Content-Type: application/json" \
  -d '{
    "template_id": 1,
    "created_user": "テストユーザー",
    "data": {
      "work_date": "2025-12-16",
      "worker_name": "山田太郎",
      "location": "東京都",
      "work_content": "テスト作業",
      "work_hours": 8
    }
  }'
```

### 3. PDF生成のテスト

ブラウザで直接アクセス：
```
http://localhost:YOUR_PORT/api/reports/1/pdf
```

---

## 🐛 トラブルシューティング

### エラー: "Excelテンプレートの解析に失敗しました"

**原因**: プレースホルダーの形式が間違っている

**解決**: プレースホルダーは `{{フィールド名:型}}` の形式で記載

### エラー: "データベースに接続できません"

**原因**: Web.configの接続文字列が間違っている

**解決**: `Web.config` の `MyDbConnection` を確認

### エラー: "テンプレートファイルが見つかりません"

**原因**: ファイルパスが間違っている、またはディレクトリが存在しない

**解決**:
```powershell
New-Item -Path "C:\app_data\templates" -ItemType Directory -Force
```

### PDF生成が遅い

**原因**: FreeSpire.XLS無料版の制限

**解決**: 商用版Spire.XLSを購入してインストール

---

## 📚 次のステップ

### Phase 2: 写真機能の実装
- 画像アップロードAPI
- Konva.jsによる画像編集
- 注釈機能

### Phase 3: フロントエンド開発
- テンプレート管理画面
- 報告書作成画面
- 報告書一覧・検索画面

### Phase 4: 応用機能
- 承認ワークフロー
- 統計・レポート機能
- PWA化（オフライン対応）

---

## 📞 サポート

質問がある場合は、開発チームまでお問い合わせください。

